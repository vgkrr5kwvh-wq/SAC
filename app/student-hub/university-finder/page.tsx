import type { Metadata } from "next";
import StudentHubToolPlaceholder from "@/components/student-hub/student-hub-tool-placeholder";

export const metadata: Metadata = { title: "University Finder", description: "Find study options that align with your academic profile and preferences." };

export default function UniversityFinderPage() {
  // TODO: Add the University Finder workflow.
  return <StudentHubToolPlaceholder title="University Finder" description="University matching guidance is being prepared." />;
}
