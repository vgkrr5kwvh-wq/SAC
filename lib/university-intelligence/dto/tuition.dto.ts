import type { TuitionScope } from "../types/tuition";
import type { UniversityPublicationStatus, UniversityVerificationStatus } from "../types/university";
export type TuitionManagementSummary = { id: string; programId: string | null; programName: string | null; scope: TuitionScope; studyLevel: string | null; degreeLevel: string | null; amount: number | null; minimumAmount: number | null; maximumAmount: number | null; currency: string | null; period: string | null; academicYear: string | null; notes: string | null; publicationStatus: UniversityPublicationStatus; verificationStatus: UniversityVerificationStatus; updatedAt: Date };
export type TuitionManagementResult = { tuition: TuitionManagementSummary[]; statistics: { total: number; published: number; draft: number; officiallyVerified: number; universityWide: number; programSpecific: number; undergraduate: number; graduate: number }; options: { studyLevels: string[]; degreeLevels: string[]; programs: Array<{ id: string; name: string }>; currencies: string[]; periods: string[]; academicYears: string[] } };
